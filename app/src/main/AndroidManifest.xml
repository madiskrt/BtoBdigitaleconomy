<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.a210311_btobdigitaleconomy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/google2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/google2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".GetStartedActivity" />
        <activity android:name=".LearnMoreActivity" />
        <activity android:name=".HomeDashboard" />
        <activity android:name=".ListActivityTemp" />
        <activity android:name=".InsertTemp" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>